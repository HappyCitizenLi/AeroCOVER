# OPEN 地面第二层中央实验

只运行 OPEN/VoFOD-Mid360，一次完整回放。ready 比例 0.0001，与 v10 第三层中央基线相同；不采用 v11/v12 的较高比例。

体素 0.25 m，地图下界 −0.375 m，地面 z=0 位于第二层 [−0.125,0.125) 中央。对应 center.z=8.8125 m、size.z=18.375 m；xy、名义上界 18 m、实际接受上界 18.375 m 不变。相比 v10 只移除最底下一层，网格相位不变。

复用原输入和现有回放／评分流程，0.25×回放，新时间协议、1.5 m 匹配、HOTA 关闭均不变。配置放在本目录 configs，默认配置不变；完整输出保存至 SU710。

`replay.py --validate` 检查唯一配置差异和第二层中心几何，`replay.py` 执行并输出 PER_SEQUENCE_ZH.md。

## 完成结果

1/1 完成，完整指标与 runtime 见 [PER_SEQUENCE_ZH.md](PER_SEQUENCE_ZH.md)。917/917 评分帧，输出／计时／完成消息／真值覆盖率均为 100%；同刻去重及输入、算法、评估器摘要检查通过。HOTA 关闭，默认配置保持第三层中央版本未改。

相比 v10：TP/FN 仍为 1656/178，IDSW/Frag 仍为 0/3；FP 4236→5180，平均 runtime 13.436→13.861 ms。ready 帧 844→843，首次 ready 时刻基本相同。这次第二层中央试验没有显示收益；两次异步回放不是确定性重复，不能据此断言全部差异均由地图下界引起。

复用 CSV 审计得到 [ERROR_AUDIT.json](ERROR_AUDIT.json)：5174/5180 个 FP 位于 `abs(z)<0.5 m`，仍以地面附近预测为主。首次匹配前 FN=172、首次匹配后 FN=6，与基线一致。高度筛选是诊断，不等同于逐点证明物理来源。
